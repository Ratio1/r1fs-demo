# =============================================================================
# R1FS + CStore Starter Kit - Environment Configuration
# =============================================================================
#
# Copy this file to .env.local and customize for your environment.
#
# This configuration works for all deployment modes:
# - r1-plugins-sandbox (local development)
# - R1EN Docker (devnet/testnet)
# - Production R1EN
#
# Only the service URLs need to change between environments!
#
# =============================================================================

# =============================================================================
# SERVICE URLS (Required)
# =============================================================================
# These URLs point to your R1FS and CStore services.
# The same code works everywhere - only these URLs change.

# CStore (ChainStore) API endpoint
# - Sandbox: http://localhost:41234
# - R1EN Docker: http://localhost:31234 (or your mapped port)
EE_CHAINSTORE_API_URL=http://localhost:41234

# R1FS (Ratio1 File System) API endpoint
# - Sandbox: http://localhost:41235
# - R1EN Docker: http://localhost:31235 (or your mapped port)
EE_R1FS_API_URL=http://localhost:41235

# =============================================================================
# AUTHENTICATION (Required)
# =============================================================================
# User credentials are stored in CStore under a dedicated hash key.
# This provides decentralized authentication without a separate database.

# Hash key for storing user credentials in CStore
# Use a unique key for each application to avoid conflicts
EE_CSTORE_AUTH_HKEY=r1-starter-auth

# Secret used for password hashing (KEEP THIS SECURE!)
# Generate a random string: openssl rand -base64 32
EE_CSTORE_AUTH_SECRET=change-me-to-a-random-secret

# =============================================================================
# FIRST TIME SETUP
# =============================================================================
# Bootstrap admin user on first run. REMOVE AFTER FIRST LOGIN!

# Uncomment to create initial admin user with this password
# EE_CSTORE_BOOTSTRAP_ADMIN_PASS=your-initial-admin-password

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Hash key for storing file metadata in CStore
# Each app instance should use a unique key
CSTORE_HKEY=r1-starter-files

# Maximum file upload size in MB (default: 10)
MAX_FILE_SIZE_MB=10

# =============================================================================
# SESSION CONFIGURATION (Optional)
# =============================================================================

# Session cookie name (default: r1-session)
# AUTH_SESSION_COOKIE=r1-session

# Session lifetime in seconds (default: 86400 = 24 hours)
# AUTH_SESSION_TTL_SECONDS=86400

# =============================================================================
# DEVELOPMENT (Optional)
# =============================================================================

# Enable debug logging
# DEBUG=true

# Node environment
# NODE_ENV=development

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# ADVANCED (Optional)
# =============================================================================

# Additional CStore peer URLs for redundancy (JSON array)
# EE_CHAINSTORE_PEERS=["http://peer1:8001", "http://peer2:8001"]
