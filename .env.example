# =============================================================================
# Ratio1 Drive Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Authentication
# -----------------------------------------------------------------------------
# CStore hash key where user credentials are stored
EE_CSTORE_AUTH_HKEY=r1-drive-auth

# Secret/pepper for password hashing (use a long random string)
EE_CSTORE_AUTH_SECRET=your-long-random-secret-change-this

# -----------------------------------------------------------------------------
# REQUIRED - First Time Setup
# -----------------------------------------------------------------------------
# Bootstrap password for creating the initial admin user
# Remove this after first successful login
EE_CSTORE_BOOTSTRAP_ADMIN_PASS=password

# -----------------------------------------------------------------------------
# Service URLs
# -----------------------------------------------------------------------------
# CStore API endpoint
EE_CHAINSTORE_API_URL=http://localhost:41234

# R1FS API endpoint
EE_R1FS_API_URL=http://localhost:41235

# -----------------------------------------------------------------------------
# Storage Configuration
# -----------------------------------------------------------------------------
# CStore hash key for file metadata
CSTORE_HKEY=r1fs-demo-dev

# -----------------------------------------------------------------------------
# Session Configuration (Optional)
# -----------------------------------------------------------------------------
# Session cookie name
# AUTH_SESSION_COOKIE=r1-session

# Session lifetime in seconds (default: 86400 = 24 hours)
# AUTH_SESSION_TTL_SECONDS=86400

# -----------------------------------------------------------------------------
# Application Configuration (Optional)
# -----------------------------------------------------------------------------
# Maximum file upload size in MB
# MAX_FILE_SIZE_MB=10

# Enable debug logging
# DEBUG=false

# Node environment
# NODE_ENV=development

# Disable Next.js telemetry
# NEXT_TELEMETRY_DISABLED=1

# -----------------------------------------------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------------------------------------------
# Additional CStore peer URLs (JSON array)
# EE_CHAINSTORE_PEERS=["http://peer1:8001", "http://peer2:8001"]
